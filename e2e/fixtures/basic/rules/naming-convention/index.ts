// Examples of incorrect code for naming-convention rule (default options)

const snake_case_variable = 1;

function Function_Name() {}

class my_class {
  Static_Property = 2;
}

interface some_interface {}

type some_type = string;

enum some_enum {}

export {};
